# API Overview

The public API is exposed through the API Gateway under `/api/**`.

## Domains

- Authentication and users
- Shelters and capacity
- Resource inventory and reservations
- Sharing workflows and transparency

Each backend service also exposes `/actuator/health` for operational health checks.
